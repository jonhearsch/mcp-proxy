# MCP Proxy Server Configuration Example
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to git - add to .gitignore

# ============================================================================
# Auth0 OAuth Configuration (Required for authentication)
# Get these values from https://manage.auth0.com
# ============================================================================

# Auth0 Application Client ID
# Found in: Dashboard → Applications → Your App → Settings → Client ID
AUTH0_CLIENT_ID=your-client-id-from-auth0

# Auth0 Application Client Secret
# Found in: Dashboard → Applications → Your App → Settings → Client Secret
AUTH0_CLIENT_SECRET=your-client-secret-from-auth0

# Auth0 Tenant Domain
# Found in: Dashboard → Settings → Domain
# Format: your-tenant.us.auth0.com or your-tenant.eu.auth0.com
AUTH0_DOMAIN=your-tenant.us.auth0.com

# Auth0 API Identifier (Audience)
# Created in: Dashboard → Applications → APIs → Your API → Settings → Identifier
AUTH0_AUDIENCE=https://your-mcp-api

# ============================================================================
# MCP Proxy Configuration
# ============================================================================

# Enable OAuth authentication
# Options: oauth_proxy (for Auth0), or leave blank to disable auth
MCP_AUTH_PROVIDER=oauth_proxy

# Public URL where your MCP Proxy is accessible
# This must match the URL you use to connect from Claude
# Examples:
#   - https://mcp.your-domain.com (via Cloudflare Tunnel)
#   - http://localhost:8080 (for local development)
MCP_BASE_URL=https://mcp.your-domain.com

# ============================================================================
# File Paths (for Docker, these point to mounted volumes)
# ============================================================================

# Path to MCP servers configuration
MCP_CONFIG_PATH=/app/mcp_config.json

# Path to authorized users file
MCP_USERS_PATH=/data/users.json

# Path to auth provider configuration
MCP_AUTH_CONFIG_PATH=/data/auth_config.json

# ============================================================================
# Server Configuration (Optional)
# ============================================================================

# Host address to bind to (0.0.0.0 = all interfaces)
MCP_HOST=0.0.0.0

# Port to listen on
MCP_PORT=8080

# Enable live config reloading (watches mcp_config.json for changes)
# Options: true, false
MCP_LIVE_RELOAD=true

# Maximum retries for loading configuration
MCP_MAX_RETRIES=3

# Initial restart delay in seconds
MCP_RESTART_DELAY=5

# ============================================================================
# Optional: URL Path Prefix for Additional Security
# ============================================================================
# Adds a random prefix to your MCP endpoint for security through obscurity
# Example: MCP_PATH_PREFIX=3434dc5d-349b-401c-8071-7589df9a0bce
# This would create: /3434dc5d-349b-401c-8071-7589df9a0bce/mcp/
# Generate with: uuidgen (on macOS/Linux) or New-Guid (PowerShell on Windows)
# MCP_PATH_PREFIX=

# ============================================================================
# Optional: Cloudflare Tunnel (for secure public access without exposing ports)
# ============================================================================
# Get this from: https://dash.cloudflare.com → Access → Tunnels → Your Tunnel
# CLOUDFLARE_TUNNEL_TOKEN=your-cloudflare-tunnel-token
