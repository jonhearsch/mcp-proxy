# MCP Proxy Server Configuration Example
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to git - add to .gitignore

# ============================================================================
# API Key Authentication (Required for authentication)
# ============================================================================

# API keys for authentication (format: "key1:client-id-1,key2:client-id-2")
# Each key maps to a client ID for tracking/logging purposes
# Example: MCP_API_KEYS="sk-abc123:letta-cloud,sk-xyz789:local-tools,sk-def456:ci-bot"
MCP_API_KEYS=your-api-key:your-client-id

# Alternative: Path to API keys JSON file
# Format: {"api-key-string": {"client_id": "client-name", "scopes": ["*"]}}
# MCP_API_KEYS_PATH=/data/api_keys.json

# ============================================================================
# MCP Server Configuration
# ============================================================================

# Path to MCP servers configuration file
MCP_CONFIG_PATH=/app/mcp_config.json

# Path to authorized users file (optional - for future user whitelisting)
MCP_USERS_PATH=/data/users.json

# ============================================================================
# Server Configuration (Optional)
# ============================================================================

# Host address to bind to (0.0.0.0 = all interfaces)
MCP_HOST=0.0.0.0

# Port to listen on
MCP_PORT=8080

# Enable live config reloading (watches mcp_config.json for changes)
# Options: true, false
MCP_LIVE_RELOAD=true

# Maximum retries for loading configuration
MCP_MAX_RETRIES=3

# Initial restart delay in seconds
MCP_RESTART_DELAY=5

# ============================================================================
# Optional: URL Path Prefix for Additional Security
# ============================================================================
# Adds a random prefix to your MCP endpoint for security through obscurity
# Example: MCP_PATH_PREFIX=3434dc5d-349b-401c-8071-7589df9a0bce
# This would create: /3434dc5d-349b-401c-8071-7589df9a0bce/mcp/
# Generate with: uuidgen (on macOS/Linux) or New-Guid (PowerShell on Windows)
# MCP_PATH_PREFIX=

# ============================================================================
# Optional: Disable Authentication (NOT RECOMMENDED)
# ============================================================================
# Only use this for local development or trusted internal networks
# MCP_DISABLE_AUTH=false

# ============================================================================
# Optional: Cloudflare Tunnel (for secure public access)
# ============================================================================
# Get this from: https://dash.cloudflare.com → Access → Tunnels → Your Tunnel
# CLOUDFLARE_TUNNEL_TOKEN=your-cloudflare-tunnel-token
