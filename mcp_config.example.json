{
  "mcpServers": {
    "time": {
      "command": "uvx",
      "args": ["mcp-server-time"]
    },
    "search": {
      "command": "npx",
      "args": ["-y", "@kevinwatt/mcp-server-searxng"],
      "env": {
        "SEARXNG_INSTANCES": "${SEARXNG_INSTANCES:-http://localhost:8082}"
      }
    }
  }
}
