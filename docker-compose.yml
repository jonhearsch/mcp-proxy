version: '3.8'

services:
  mcp-proxy:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./mcp_config.json:/app/mcp_config.json:ro
      - ./data:/data
    environment:
      - MCP_CONFIG_PATH=/app/mcp_config.json
      - LOG_LEVEL=debug
